module ChoiceConstrainedByLaw where

open import Agda.Builtin.Equality

select : {A : Set} → A → A → A
select = {!!}

select-right : {A : Set} → (x : A) → (y : A) → select x y ≡ y
select-right = {!!}
